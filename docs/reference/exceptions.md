# Exceptions

All stable public failures derive from `OpenNASRError`. Catch narrower
subclasses when an application can recover from missing cycles, records,
tables, invalid routes, schema drift, or ambiguous identifiers.

```{eval-rst}
.. automodule:: openNASR.exceptions
```
