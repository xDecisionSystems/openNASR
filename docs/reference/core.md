# Table discovery and loading

The {doc}`../types/nasr-cycle` page documents `NASR`, `CycleManager`, and cycle
metadata. This page covers the lower-level lazy CSV table repository used by
the facade.

## Lazy CSV tables

```{eval-rst}
.. automodule:: openNASR.tables
```
