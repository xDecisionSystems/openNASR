# Listing entities in an ARTCC

This example uses Cleveland Center (`ZOB`) and prints the names of airports,
fixes, and intersecting airways inside its high-altitude boundary.

Run it with:

```bash
python examples/print_airspace_entities.py
```

## Script

```{literalinclude} ../../examples/print_airspace_entities.py
:language: python
:linenos:
```
